

<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
        <%= link_to 'Private Events', root_path, class: 'navbar-brand' %>
         <% if session[:current_user] %>
         <p> <%= link_to "Welcome, #{session[:current_user]['name'].capitalize}", user_path(session[:current_user]['id']) %></p>
          <%= link_to 'Sign Out', sign_out_path, class: 'btn btn-link' %>
        <% else %>
                  <%= link_to 'Sign In', sign_in_path, class: 'btn btn-link' %>
                  <%= link_to 'Sign Up', new_user_path, class: 'btn btn-link' %>
        <% end %>
  </div>
</nav>